# Brand assets

Minimal **placeholder** brand images so HACS brand validation passes without
listing the domain in the `home-assistant/brands` repo:

- `icon.png` (256×256) / `icon@2x.png` (512×512)
- `logo.png` / `logo@2x.png`

These are a plain bus glyph on the line-10 brand blue — deliberately simple.
Replace them with a proper icon/logo before any wider release (and, if
submitting to core HACS, mirror them into `home-assistant/brands`).
