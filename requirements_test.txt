# pytest-homeassistant-custom-component is welded to one Home Assistant core version — this pin
# pulls homeassistant==2026.7.x transitively. Bump both together (the HAcc release notes name the
# HA version each release targets).
pytest-homeassistant-custom-component==0.13.345
pytest-cov>=5.0.0
ruff==0.15.20
mypy>=1.11.0
